# Core
numpy==1.26.4
tqdm==4.66.4
regex==2024.5.15
pyyaml==6.0.1
fire==0.6.0
packaging==24.1

# Experiment tooling
pandas==2.2.2
scikit-learn==1.4.2
matplotlib==3.8.4
einops==0.8.0
safetensors==0.4.3

# Logging / optional
wandb==0.17.4

# Notes:
# - PyTorch is intentionally not pinned here; install it explicitly (see README)
# - Future Lens and ROME will be vendored/integrated as submodules or separate installs
